<!--
Заголовок PR = заголовок коммита + ключ задачи в конце:
    feat(tools): add "write_document" tool spec (OVE-42)
Подробнее — CONTRIBUTING.md.
-->

## Что и зачем

<!-- Что меняется и почему. Если правился контракт (tool spec, схема БД,
     WS-протокол) — что именно разъедется, если не накатить это вместе. -->

## Задача

<!-- Не удаляй строку ниже и подставь реальный ключ.
     (OVE-<n>) в заголовке PR нужен Jira; полный URL здесь — CodeRabbit'у,
     чтобы подтянуть описание и критерии приёмки. Это две разные связки. -->
Closes https://<ваш-домен>.atlassian.net/browse/OVE-<n>

## Чек-лист

- [ ] `pre-commit run --all-files` проходит (ruff + mypy)
- [ ] `pytest` — зелёный
- [ ] при изменении SQLAlchemy-моделей: модель импортирована в `libs/db/models/__init__.py` и добавлена Alembic-миграция (в этом же PR)
- [ ] при изменении контракта (tool spec, WS-протокол, эндпоинт): парные файлы обновлены одним коммитом, решение зафиксировано в описании или в `.claude/knowledge/`
- [ ] один PR — одна задача, постороннего рефакторинга «заодно» нет
